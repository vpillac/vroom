annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
application.title=VroomRouting
application.vendor=Victor Pillac
auxiliary.org-netbeans-modules-projectimport-eclipse-core.key=src=src;src=test;prj=jVRP;prj=VroomUtilities;file=lib/log4j-1.2.15.jar;prj=VroomHeuristics;ant=libs.Gurobi.classpath;output=bin;
auxiliary.org-netbeans-modules-projectimport-eclipse-core.project=../../VroomRouting
auxiliary.org-netbeans-modules-projectimport-eclipse-core.timestamp=1282570810017
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/VroomRouting.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.log4j-1.2.15.jar=../../VroomRouting/lib/log4j-1.2.15.jar
file.reference.VroomRouting-src=../../VroomRouting/src
file.reference.VroomRouting-test=../../VroomRouting/test
includes=**
jar.compress=false
javac.classpath=\
    ${reference.jVRP.jar}:\
    ${reference.VroomUtilities.jar}:\
    ${file.reference.log4j-1.2.15.jar}:\
    ${reference.VroomHeuristics.jar}:\
    ${libs.Gurobi_301.classpath}:\
    ${libs.SSJ.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.processorpath=\
    ${javac.classpath}
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javac.test.processorpath=\
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.jVRP=../jVRP
project.VroomHeuristics=../VroomHeuristics
project.VroomUtilities=../VroomUtilities
reference.jVRP.jar=${project.jVRP}/dist/jVRP.jar
reference.VroomHeuristics.jar=${project.VroomHeuristics}/dist/VroomHeuristics.jar
reference.VroomUtilities.jar=${project.VroomUtilities}/dist/VroomUtilities.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=${file.reference.VroomRouting-src}
test.dir=${file.reference.VroomRouting-test}
